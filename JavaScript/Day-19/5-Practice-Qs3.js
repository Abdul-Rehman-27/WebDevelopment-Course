// QS: Create a function that gives us the average of 3 numbers

function calculateAverage(num1, num2, num3) {
    let avg = (num1 + num2 + num3) / 3;
    console.log(`Average of ${num1}, ${num2} and ${num3} is: ${avg}`);
}

calculateAverage(0, 1, 2);