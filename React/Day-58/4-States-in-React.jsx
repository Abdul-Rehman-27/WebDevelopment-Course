//------------State in React-------------
/*
-The state is a built-in react object that is used to contain data or info about the component. A component's can change over time; whenever it changes, the component re-renders.

-When we want that something is being changed in component and we want to re-render it then we use state
*/

//--Some key Point
/**
-Props are immutable. If their value changes component will not re-render. Components only re-render when state changes.
*/

